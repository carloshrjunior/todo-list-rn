import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  filterText: {
    fontSize: 16,
    fontWeight: "bold",

    marginRight: 5,
  },

  filterTasksNumberContainer: {
    backgroundColor: "#333",

    borderRadius: 50,
  },

  filterTasksNumber: {
    color: "#fff",

    fontSize: 16,
    fontWeight: "bold",

    paddingVertical: 1,
    paddingHorizontal: 9 ,
  }
})

export default styles;